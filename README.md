## mvel-lsp

The `mvel-lsp` repository contains the following components:

- `mvel3-completion` – Code completion support for MVEL3
- `mvel3-lsp-server` – An LSP (Language Server Protocol) server that exposes the code completion functionality
- `client` – Builds the VS Code extension `vscode-extension-mvel3-editor`
